# TriP - Virtual Tabletop

> Mobile-first VTT (Virtual Tabletop) для настольных ролевых игр. 
> Мастер создаёт комнату, игроки подключаются по коду — P2P без сервера.

## Возможности

- 🗺️ Гексагональная боевая карта с токенами
- ⚔️ Создание персонажей с классами, расами, экипировкой
- 🎲 Броски кубиков и проверки навыков
- 📱 Полностью адаптивный мобильный интерфейс
- 🔗 P2P подключение без центрального сервера

## Стек

- [Vue 3](https://vuejs.org/) + [Vite](https://vitejs.dev/)
- [Pinia](https://pinia.vuejs.org/) для состояния
- [PeerJS](https://peerjs.com/) для p2p-соединений
- [UnoCSS](https://uno.antfu.me/) для стилей

## Скрипты

```powershell
npm install        # установка зависимостей
npm run dev        # старт dev-сервера
npm run build      # production-сборка
npm run preview    # предпросмотр сборки
npm run test:unit  # unit-тесты (Vitest)
npm run lint       # ESLint + форматтер
```

## Структура проекта

```
src/
├── components/          # Vue компоненты
│   ├── battle/         # Боевая карта, лог событий
│   ├── character/      # Создание и отображение персонажей
│   ├── equipment/      # Экипировка и инвентарь
│   ├── master/         # Инструменты мастера
│   ├── layout/         # UI layout и адаптивность
│   └── shared/         # Переиспользуемые компоненты
├── views/              # Страницы приложения
├── stores/             # Pinia stores
├── utils/              # Утилиты
├── data/               # JSON игровые данные
└── router/             # Vue Router
```

> 📖 Каждая папка содержит README.md с подробным описанием для AI.

## Быстрый старт

1. `npm run dev` — откройте приложение в двух вкладках
2. Первая вкладка: `/master` → «Создать комнату»
3. Скопируйте код комнаты
4. Вторая вкладка: вставьте код, присоединитесь как игрок
5. Создайте персонажа и начните игру

## Настройка PeerJS

По умолчанию используется `0.peerjs.com`. Для production поднимите свой PeerServer и настройте `src/stores/session.js` (`peerConfig`).
